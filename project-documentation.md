# مشروع نظام تقييم BTEC باستخدام الذكاء الاصطناعي

## نظرة عامة
نظام تقييم ذكي لمقررات Pearson BTEC International للمستويين 2 و3 في الأعمال. يستخدم الذكاء الاصطناعي لتقييم إجابات الطلبة تلقائياً بناءً على معايير BTEC، مع توفير لوحة تحكم للمعلمين لعرض النتائج والإحصائيات.

## الأهداف
1. تقييم إجابات الطلبة تلقائياً باستخدام الذكاء الاصطناعي (نموذج GPT-4o-mini).
2. توفير تحليل مفصل للإجابات مع تحديد المعايير المحققة (P1, P2, M1, D1, إلخ).
3. إنشاء لوحة تحكم تفاعلية للمعلمين لعرض النتائج والإحصائيات.
4. دعم تقييم الوحدات الدراسية التالية:
   - الوحدة 2: مؤسسات الأعمال.
   - الوحدة 4: خطة التسويق.
   - الوحدة 7: اتخاذ قرارات الأعمال.

## هيكل المشروع
### 1. Backend (FastAPI)
- **اللغة:** Python 3.9+
- **الإطار:** FastAPI
- **قاعدة البيانات:** SQLite (للبداية، يمكن الترقية لاحقاً)
- **الذكاء الاصطناعي:** نموذج GPT-4o-mini من OpenAI

#### الملفات الرئيسية:
- `main.py`: نقطة دخول التطبيق، يحتوي على تعريف التطبيق والروابط العامة.
- `routes/btec.py`: يحتوي على نقاط النهاية (endpoints) الخاصة بتقييم BTEC.
- `services/evaluator.py`: خدمة تقييم الإجابات باستخدام الذكاء الاصطناعي.
- `models/schemas.py**: نماذج البيانات (Pydantic).
- `utils/helpers.py**: دوال مساعدة.

### 2. Frontend (Flutter)
- **اللغة:** Dart
- **الإطار:** Flutter
- **التصميم:** Material Design مع تخصيصات لتكون جذابة ومبتكرة ورسمية.

#### الملفات الرئيسية:
- `lib/main.dart`: نقطة دخول التطبيق.
- `lib/pages/dashboard.dart`: لوحة تحكم المعلم.
- `lib/pages/evaluation.dart`: صفحة التقييم.
- `lib/services/api.dart`: خدمة الاتصال بالـ Backend.
- `lib/widgets/`: مكونات واجهة مخصصة.

### 3. قاعدة البيانات (SQLite)
- **الجدول:** `results`
  - `id`: معرف فريد.
  - `timestamp`: وقت التقييم.
  - `student_name`: اسم الطالب.
  - `unit`: الوحدة الدراسية.
  - `criteria`: المعيار المحقق (P1, P2, M1, D1, ...).
  - `grade`: الدرجة (نفس المعيار، أو يمكن إضافة تحويل لدرجات رقمية).
  - `feedback`: التعليق التغذوي.

## شرح الوحدات الدراسية والأسئلة المستهدفة

### الوحدة 2: مؤسسات الأعمال
**نوع الأسئلة:** تحليل شركتين متباينتين (مثال: شركة جليم كارز وشركة أخرى هادفة للربح في القطاع الثانوي).
**المعايير المستهدفة:**
- A.P1: شرح أهداف وغايات الشركات.
- A.P2: وصف أنشطة الشركات.
- A.M1: تحليل فعالية الأنشطة في تحقيق الأهداف.
- A.D1: تقييم فعالية الأنشطة.
- B.P3: تحليل المجالات الوظيفية.
- B.P4: وصف الروابط بين المجالات الوظيفية.
- B.M2: تحليل الروابط بين المجالات الوظيفية.
- B.D2: تقييم أهمية الروابط بين المجالات الوظيفية.

### الوحدة 4: خطة التسويق
**نوع الأسئلة:** إعداد خطة تسويقية لمؤسسة محددة (مثال: ديسكونت ديزاينرشوبينج).
**المعايير المستهدفة:**
- A.P1: شرح مفاهيم التسويق.
- A.P2: وصف مزايا وعيوب الأساليب الترويجية.
- A.M1: تحليل بيانات أبحاث السوق.
- A.D1: تقييم أهمية بناء هوية العلامة التجارية.
- B.P3: إعداد خطة تسويقية.
- B.P4: تحديد أهداف الخطة التسويقية.
- B.P5: تحديد معايير نجاح الخطة.
- B.M2: تحليل الخطة التسويقية.
- B.D2: تقييم الخطة التسويقية.

### الوحدة 7: اتخاذ قرارات الأعمال
**نوع الأسئلة:** دراسة سيناريو عملي (مثال: مشروع أزياء أورال المستدامة) واتخاذ قرار بين خيارين.
**المعايير المستهدفة:**
- A.P1: شرح العوامل المؤثرة على القرار.
- A.P2: وصف المخاطر والفرص.
- A.M1: تحليل الخيارات.
- A.D1: تقييم الخيارات.
- B.P3: تحليل الموارد المطلوبة.
- B.P4: وصف التشريعات المؤثرة.
- B.M2: تحليل الموارد والتشريعات.
- B.D2: تقييم أهمية الموارد والتشريعات.
- C.P5: إعداد تقرير رسمي.
- C.P6: تقديم توصية مدعومة.
- C.M3: تحليل البيانات المالية.
- C.D3: تقييم القرار بناءً على البيانات.
- D.P7: إعداد عرض تقديمي.
- D.P8: إعداد ملاحظات المتحدث.
- D.M4: تحليل العرض التقديمي.
- D.D4: تقييم العرض التقديمي.

## تصميم لوحة الإدارة (Teacher Dashboard)

### المبادئ التصميمية:
1. **الجاذبية البصرية:** استخدام ألوان متدرجة حديثة، مع تأثيرات ظل وحركات سلسة.
2. **الابتكار:** عرض البيانات بطريقة تفاعلية، مع مخططات بيانية متحركة.
3. **الرسمية:** الحفاظ على مظهر احترافي يناسب بيئة التعليم.

### الألوان المقترحة:
- اللون الأساسي: أزرق داكن (#1A237E) متدرج إلى أزرق متوسط (#283593).
- اللون الثانوي: سماوي (#00BCD4).
- ألوان الحالة: أخضر (#4CAF50) للنجاح، برتقالي (#FF9800) للتحذير.

### المكونات الرئيسية للوحة التحكم:

#### 1. شريط الإحصائيات العلوي (Stat Cards)
- بطاقات صغيرة تعرض إحصائيات سريعة:
  - عدد التقييمات اليومية.
  - عدد الطلبة النشطين.
  - متوسط الدرجات.
  - نسبة النجاح.

#### 2. مخطط توزيع الدرجات (Pie Chart)
- مخطط دائري يعرض توزيع الدرجات (ممتاز، جيد جداً، جيد، ضعيف).

#### 3. مخطط التقدم الزمني (Line Chart)
- مخطط خطي يظهر تقدم الدرجات عبر الوقت.

#### 4. قائمة التقييمات الأخيرة (Recent Evaluations)
- جدول يعرض آخر التقييمات مع معلومات الطالب والوحدة والمعيار المحقق والدرجة.

#### 5. تفاصيل الطالب (Student Details)
- عند النقر على طالب، تظهر نافذة منبثقة مع تفاصيل أدائه في كل معيار.

### التفاعلات:
- تحريك المخططات عند التمرير.
- تأثيرات hover على البطاقات.
- انتقالات سلسة بين الصفحات.

## خطوات التنفيذ المقترحة للذكاء الاصطناعي (Lovable AI)

### المرحلة 1: إعداد Backend
1. إنشاء هيكل المشروع (المجلدات والملفات).
2. كتابة كود FastAPI في `main.py` و `routes/btec.py`.
3. إنشاء خدمة التقييم بالذكاء الاصطناعي في `services/evaluator.py`.
4. تكوين قاعدة البيانات SQLite.

### المرحلة 2: إعداد Frontend
1. إنشاء مشروع Flutter.
2. تصميم لوحة التحكم بناءً على المواصفات أعلاه.
3. إنشاء خدمة الاتصال بالـ Backend في `lib/services/api.dart`.
4. بناء الصفحات الرئيسية (لوحة التحكم، صفحة التقييم).

### المرحلة 3: التكامل
1. ربط Frontend مع Backend.
2. اختبار نقطة النهاية `/evaluate` بنماذج إجابات حقيقية.
3. عرض النتائج في لوحة التحكم.

### المرحلة 4: التحسينات
1. إضافة تحليل إضافي (مثل نسبة الانتحال، احتمالية كتابة AI للإجابة).
2. تحسين واجهة المستخدم بإضافة تأثيرات وحركات.
3. تحسين دقة نموذج الذكاء الاصطناعي عن طريق ضبط الـ prompt.

## أمثلة على كود مهم

### Backend: تقييم الإجابة باستخدام الذكاء الاصطناعي
```python
# في services/evaluator.py
async def evaluate_answer(answer: str, unit: str) -> dict:
    # بناء الـ prompt بناءً على الوحدة الدراسية
    prompt = build_prompt(unit, answer)
    # استدعاء نموذج الذكاء الاصطناعي
    response = await openai.ChatCompletion.acreate(
        model="gpt-4o-mini",
        messages=[{"role": "user", "content": prompt}]
    )
    # تحليل الرد واستخراج المعايير والدرجة
    result = parse_response(response)
    return result
	
	// في lib/widgets/stat_card.dart
class StatCard extends StatelessWidget {
  final String title;
  final String value;
  final Color color;

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(16),
      decoration: BoxDecoration(
        gradient: LinearGradient(
          colors: [color.withOpacity(0.7), color],
          begin: Alignment.topLeft,
          end: Alignment.bottomRight,
        ),
        borderRadius: BorderRadius.circular(12),
        boxShadow: [
          BoxShadow(
            color: color.withOpacity(0.5),
            blurRadius: 8,
            offset: Offset(0, 4),
          ),
        ],
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(title, style: TextStyle(color: Colors.white70)),
          SizedBox(height: 8),
          Text(value, style: TextStyle(color: Colors.white, fontSize: 24, fontWeight: FontWeight.bold)),
        ],
      ),
    );
  }
}

الخلاصة
هذا المشروع يهدف إلى أتمتة تقييم إجابات طلبة BTEC باستخدام الذكاء الاصطناعي، مع توفير واجهة غنية وجذابة للمعلمين. سيكون النظام قادراً على توفير وقت المعلمين وتحسين دقة التقييم.

يمكن للذكاء الاصطناعي في Lovable تنفيذ هذا المشروع خطوة بخطة بناءً على الشرح أعلاه.

text

هذا السكربت يمكن تقديمه للذكاء الاصطناعي في Lovable ليقوم ببناء المشروع.
دليل مشروع نظام تقييم BTEC الذكي
نظرة عامة للمساعد الذكي في Lovable
🎯 ملخص المشروع: نظام تقييم BTEC المدعوم بالذكاء الاصطناعي
📊 الهدف الأساسي:
بناء نظام ذكي لتقييم إجابات طلبة Pearson BTEC International للمستوى 2 و3 في الأعمال، باستخدام الذكاء الاصطناعي لتحديد المعايير المحققة (P1, P2, M1, D1) تلقائياً.

🏗️ الهيكل التقني
Backend (Python + FastAPI)
python
# هيكل الملفات المطلوب:
backend/
├── app/
│   ├── main.py              # نقطة دخول التطبيق
│   ├── routes/
│   │   └── btec.py          # مسارات تقييم BTEC
│   ├── services/
│   │   └── evaluator.py     # خدمة الذكاء الاصطناعي
│   └── models/
│       └── schemas.py       # نماذج البيانات
├── requirements.txt
└── btec_results.db         # قاعدة البيانات
Frontend (Flutter + Dart)
dart
# هيكل الملفات المطلوب:
frontend/
├── lib/
│   ├── main.dart           # نقطة دخول التطبيق
│   ├── pages/
│   │   ├── dashboard.dart  # لوحة التحكم
│   │   └── evaluation.dart # صفحة التقييم
│   ├── widgets/
│   ├── services/
│   └── models/
└── pubspec.yaml
📚 تفاصيل الوحدات الدراسية المستهدفة
الوحدة 2: مؤسسات الأعمال
نوع التقييم: تقرير تحليلي
المهارات المستهدفة:

شرح الأهداف والغايات (A.P1)

وصف الأنشطة (A.P2)

تحليل الفعالية (A.M1)

التقييم الشامل (A.D1)

مثال عملي:

text
"قم بتحليل شركة 'جليم كارز' وشركة أخرى هادفة للربح في القطاع الثانوي، 
وابحث في أهدافها المالية وغير المالية، وأنشطتها، وفعاليتها."
الوحدة 4: خطة التسويق
نوع التقييم: خطة تسويقية
المهارات المستهدفة:

فهم مفاهيم التسويق (A.P1)

تحليل بيانات السوق (A.P2)

تطبيق أبحاث السوق (A.M1)

بناء الهوية التجارية (A.D1)

مثال عملي:

text
"أعد خطة تسويقية لشركة 'ديسكونت ديزاينرشوبينج' تتضمن:
الأهداف، الرسالة، الميزانية، الجداول الزمنية، ومعايير النجاح."
الوحدة 7: اتخاذ قرارات الأعمال
نوع التقييم: تقرير قرار استراتيجي
المهارات المستهدفة:

تحليل البيانات (C.P5)

اتخاذ القرارات (C.P6)

التخطيط الاستراتيجي (C.M3)

التقييم الشامل (C.D3)

مثال عملي:

text
"بناءً على بيانات مشروع 'أزياء أورال المستدامة'، أوصِ بالخيار الأمثل 
بين متجر كبير في مركز تسوق أو متجر صغير في المدينة مع التحليل المالي."
🤖 نموذج التقييم بالذكاء الاصطناعي
Prompt Template للتقييم:
python
prompt = """
أنت مقيّم معتمد لبرنامج BTEC Business.
قم بتقييم إجابة الطالب التالية وتحديد المعيار المناسب:

المعايير المتاحة:
P1: شرح الغرض من المعلومات التجارية
P2: وصف أنواع المعلومات التجارية المختلفة
M1: تحليل كيفية استخدام المعلومات التجارية
D1: تقييم فعالية المعلومات التجارية

إجابة الطالب:
{student_answer}

المطلوب:
1. حدد المعيار المحقق (P1, P2, M1, D1, أو NYA)
2. قدم تقييماً مختصراً
3. حدد نقاط القوة والضعف

إرجاع JSON فقط بالتنسيق:
{
    "criteria": "P1",
    "score": 85,
    "feedback": "تغذية راجعة",
    "strengths": ["النقطة 1", "النقطة 2"],
    "weaknesses": ["النقطة 1", "النقطة 2"]
}
"""
🎨 تصميم لوحة التحكم المتقدمة
🎨 نظام الألوان:
dart
// اللون الأساسي المتدرج
const Color primaryColor = Color(0xFF1A237E);  // أزرق داكن
const Color secondaryColor = Color(0xFF283593); // أزرق متوسط
const Color accentColor = Color(0xFF00BCD4);    // سماوي
const Color successColor = Color(0xFF4CAF50);   // أخضر
const Color warningColor = Color(0xFFFF9800);   // برتقالي
📊 مكونات واجهة المستخدم:
1. البطاقات الإحصائية التفاعلية:
dart
// بطاقة إحصائية متحركة
class AnimatedStatCard extends StatelessWidget {
  final String title;
  final String value;
  final IconData icon;
  final Color color;
  
  // تصميم مع تأثيرات ثلاثية الأبعاد وتدرجات لونية
}
2. المخططات البيانية:
مخطط دائري متحرك لتوزيع الدرجات

مخطط خطي للتقدم عبر الوقت

مخطط شريطي للمقارنات بين الطلبة

3. جدول الطلبة التفاعلي:
dart
// جدول مع تأثيرات hover وتصفية ذكية
DataTable(
  columns: [
    DataColumn(label: Text('اسم الطالب')),
    DataColumn(label: Text('الوحدة')),
    DataColumn(label: Text('المعيار')),
    DataColumn(label: Text('الدرجة')),
    DataColumn(label: Text('التاريخ')),
  ],
  rows: students.map((student) {
    return DataRow(
      cells: [
        DataCell(StudentAvatar(student)),
        DataCell(UnitBadge(student.unit)),
        DataCell(CriteriaIndicator(student.criteria)),
        DataCell(AnimatedScore(student.score)),
        DataCell(DateWidget(student.date)),
      ],
      onSelectChanged: (_) => showStudentDetails(student),
    );
  }).toList(),
)
4. محرر التقييم الذكي:
dart
// محرر نصوص مع تمييز بناء الجملة
CodeEditor(
  controller: answerController,
  language: 'arabic',
  theme: 'vs-dark',
  autoComplete: true,
  lineNumbers: true,
  highlightKeywords: {
    'تحليل': accentColor,
    'تقييم': successColor,
    'شرح': primaryColor,
    'وصف': warningColor,
  },
)
✨ التأثيرات البصرية:
تأثيرات الانتقال:

Fade-in للأجسام الجديدة

Slide-up للبطاقات

Scale للمخططات

تأثيرات Hover:

ارتفاع البطاقات عند التمرير

تغيير الألوان تدريجياً

ظلال متحركة

الرسوم المتحركة:

تحميل دائري متدرج

عدادات متحركة للأرقام

رسوم بيانية متحركة

🔧 واجهات البرمجة (API Endpoints)
Backend API:
python
# المسارات الرئيسية
POST   /api/v1/btec/evaluate     # تقييم الإجابة
GET    /api/v1/btec/result/{id}  # الحصول على نتيجة
GET    /api/v1/btec/all          # كل النتائج
GET    /api/v1/btec/criteria     # المعايير
POST   /api/v1/btec/upload       # رفع الملفات
GET    /api/v1/health            # فحص حالة النظام
نموذج طلب التقييم:
json
{
  "student_name": "سارة أحمد",
  "unit": "الوحدة 4: خطة التسويق",
  "answer": "الإجابة الكاملة للطالب..."
}
نموذج الاستجابة:
json
{
  "id": 123,
  "student": "سارة أحمد",
  "unit": "الوحدة 4",
  "criteria": "M1",
  "score": 85,
  "feedback": "تحليل قوي مع أمثلة مناسبة",
  "strengths": ["استخدام أمثلة عملية", "تحليل منطقي"],
  "weaknesses": ["احتاج لمزيد من المراجع"],
  "timestamp": "2024-01-15T10:30:00Z"
}
📱 مكونات Flutter الأساسية
1. صفحة لوحة التحكم الرئيسية:
dart
class TeacherDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: CustomAppBar(
        title: 'لوحة تحكم BTEC الذكية',
        actions: [
          NotificationBell(),
          UserProfileMenu(),
        ],
      ),
      body: ResponsiveLayout(
        desktop: DesktopDashboard(),
        tablet: TabletDashboard(),
        mobile: MobileDashboard(),
      ),
      drawer: NavigationDrawer(),
      floatingActionButton: QuickEvaluateButton(),
    );
  }
}
2. خدمة الاتصال بالـ Backend:
dart
class BtecService {
  static const String baseUrl = "http://localhost:8000/api/v1/btec";
  
  static Future<EvaluationResult> evaluate({
    required String studentName,
    required String unit,
    required String answer,
  }) async {
    final response = await http.post(
      Uri.parse('$baseUrl/evaluate'),
      headers: {'Content-Type': 'application/json'},
      body: jsonEncode({
        'student_name': studentName,
        'unit': unit,
        'answer': answer,
      }),
    );
    
    return EvaluationResult.fromJson(jsonDecode(response.body));
  }
}
🚀 خطوات التنفيذ المطلوبة من Lovable AI:
المرحلة 1: إعداد البيئة
إنشاء هيكل الملفات كما هو موضح

تثبيت المتطلبات (Python + Flutter)

إعداد قاعدة البيانات SQLite

المرحلة 2: تطوير Backend
كتابة app/main.py مع FastAPI

تطوير app/routes/btec.py مع نقاط النهاية

بناء خدمة الذكاء الاصطناعي في app/services/evaluator.py

إنشاء نماذج البيانات في app/models/schemas.py

المرحلة 3: تطوير Frontend
إعداد مشروع Flutter جديد

بناء صفحة لوحة التحكم الرئيسية

تطوير واجهة التقييم التفاعلية

إنشاء الخدمات والـ API clients

تطبيق التصميم والأنيميشن المطلوبة

المرحلة 4: التكامل والاختبار
ربط Frontend مع Backend

اختبار جميع الميزات

تحسين الأداء والتجربة

المرحلة 5: التحسينات المتقدمة
إضافة تحليل الانتحال

دعم رفع الملفات (PDF, DOCX)

إضافة التقارير والإحصائيات

تحسين دقة نموذج الذكاء الاصطناعي

📋 المتطلبات التقنية:
Backend:
Python 3.9+

FastAPI

SQLite3

OpenAI API (GPT-4o-mini)

Uvicorn

Frontend:
Flutter 3.0+

Dart 2.19+

HTTP package

Animations package

Charts package

🎯 المخرجات المتوقعة:
من Backend:
✅ نظام تقييم ذكي بالكامل
✅ قاعدة بيانات آمنة
✅ واجهات برمجة RESTful
✅ تكامل مع OpenAI API

من Frontend:
✅ لوحة تحكم جذابة ومبتكرة
✅ واجهة تقييم تفاعلية
✅ نظام تقارير متكامل
✅ تصميم متجاوب لجميع الأجهزة


📞 معلومات الاتصال للمتابعة:
المطور: Hamza Al-Manaseer

المشروع: BTEC Assessment Engine

النوع: نظام تعليمي ذكي

الهدف: أتمتة تقييم إجابات BTEC

ملاحظة للمساعد الذكي في Lovable:
يرجى البدء ببناء الهيكل الأساسي أولاً، ثم تطوير Backend، يليه Frontend، مع التركيز على:

دقة التقييم بالذكاء الاصطناعي

جمالية واجهة المستخدم

سهولة الاستخدام

السرعة والأداء

