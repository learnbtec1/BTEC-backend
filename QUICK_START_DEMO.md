# ⚡ Quick Start Demo - 3 Minutes

## 🎯 تشغيل سريع في 3 دقائق

### الطريقة الأسرع (خطوة واحدة):

```bash
./quick-deploy.sh
```

اختر رقم **1**

---

## 📸 لقطات شاشة متوقعة

### 1. Backend يعمل ✅

عند تشغيل Backend، ستظهر هذه الرسالة:

```
INFO:     Will watch for changes in these directories: ['/path/to/BTEC-backend/backend']
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [xxxxx] using WatchFiles
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

✅ **معنى ذلك:** Backend جاهز على http://localhost:8000

---

### 2. Flutter يعمل ✅

عند تشغيل Flutter، ستظهر:

```
Launching lib/main.dart on Chrome in debug mode...
Building application for the web...                                        
Compiling lib/main.dart for the Web...                      45.2s
Waiting for connection from debug service on Chrome...                     8ms
This app is linked to the debug service: ws://127.0.0.1:xxxxx/ws

Flutter Web plugin registrant... done.
```

ثم سيفتح المتصفح تلقائياً!

✅ **معنى ذلك:** التطبيق يعمل في المتصفح

---

### 3. شاشة تسجيل الدخول 🔐

**ما سترى:**
```
┌─────────────────────────────────────────┐
│                                         │
│        🎓 BTEC Smart Platform           │
│                                         │
│   ┌───────────────────────────────┐    │
│   │ 📧 Email                       │    │
│   └───────────────────────────────┘    │
│                                         │
│   ┌───────────────────────────────┐    │
│   │ 🔒 Password                    │    │
│   └───────────────────────────────┘    │
│                                         │
│        [ تسجيل الدخول ]                │
│                                         │
└─────────────────────────────────────────┘
```

**جرّب:**
- Email: `test@btec.com`
- Password: `password123`

---

### 4. Dashboard 📊

**ما سترى بعد الدخول:**

```
┌─────────────────────────────────────────────────────────┐
│  BTEC Smart Platform          👤 Student Name    🚪     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  📊 Dashboard                                           │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐ │
│  │   ✍️      │  │   🎤      │  │   📋      │  │  ⚙️     │ │
│  │ تقييم     │  │  صوتي    │  │ النتائج  │  │ إعدادات│ │
│  │  نصي     │  │          │  │          │  │        │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 5. شاشة التقييم ✍️

**ما سترى عند الضغط على "تقييم نصي":**

```
┌─────────────────────────────────────────────┐
│  Assessment                                 │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ اكتب إجابتك هنا...                 │   │
│  │                                     │   │
│  │                                     │   │
│  │                                     │   │
│  └─────────────────────────────────────┘   │
│                                             │
│         [ تقييم الإجابة ]                   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📊 النتيجة: 85%                     │   │
│  │ ✅ حالة: ممتاز                      │   │
│  │ 📝 التشابه: 0.85                    │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 🧪 اختبار سريع

### اختبار Backend فقط:

```bash
# تشغيل Backend
cd backend
uv run fastapi dev app/main.py

# في terminal جديد، اختبر API:
curl http://localhost:8000/api/v1/health
```

**النتيجة المتوقعة:** رسالة تأكيد أن الـ API يعمل

---

### اختبار Text Evaluation:

```bash
curl -X POST http://localhost:8000/api/v1/btec/evaluate/text \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "student_answer=Hello World&model_answer=Hello World"
```

**النتيجة المتوقعة:**
```json
{
  "status": "ok",
  "data": {
    "similarity": 1.0,
    "levenshtein_ratio": 1.0
  }
}
```

---

## 🎬 Demo Flow

### السيناريو الكامل (دقيقتين):

1. **شغّل Backend** (30 ثانية)
   ```bash
   cd backend && uv run fastapi dev app/main.py
   ```

2. **شغّل Flutter** (30 ثانية)
   ```bash
   cd Flutter && flutter run -d chrome
   ```

3. **سجل دخول** (10 ثوانٍ)
   - Email: test@btec.com
   - Password: password123

4. **اذهب لشاشة Assessment** (5 ثوانٍ)
   - اضغط على بطاقة "تقييم نصي"

5. **اختبر التقييم** (30 ثانية)
   - اكتب: "The quick brown fox jumps"
   - اضغط "تقييم الإجابة"
   - شاهد النتيجة!

6. **تحقق من النتائج** (15 ثانية)
   - اذهب لشاشة "النتائج"
   - شاهد قائمة التقييمات

---

## ✅ Checklist - هل يعمل؟

- [ ] Backend يعمل على http://localhost:8000
- [ ] `/docs` يفتح ويعرض Swagger UI
- [ ] Flutter يفتح في المتصفح
- [ ] شاشة Login تظهر بشكل صحيح
- [ ] يمكنني تسجيل الدخول
- [ ] Dashboard يظهر 4 بطاقات
- [ ] Assessment screen يفتح
- [ ] يمكنني كتابة نص
- [ ] زر "تقييم" يعمل
- [ ] النتيجة تظهر
- [ ] Results screen يعرض القائمة

إذا أتممت كل النقاط: **مبروك! التطبيق يعمل! 🎉**

---

## 🚨 مشكلة شائعة وحلها السريع

### "Port already in use"

**الحل:**
```bash
# على Linux/Mac:
lsof -ti:8000 | xargs kill -9

# على Windows:
netstat -ano | findstr :8000
taskkill /PID <PID_NUMBER> /F

# أو استخدم port مختلف:
uv run fastapi dev app/main.py --port 8001
```

---

## 📱 على الموبايل؟

```bash
# للـ Android
flutter run -d android

# للـ iOS
flutter run -d ios
```

**ملاحظة:** تحتاج Android Studio أو Xcode مثبت

---

## 🎊 النتيجة النهائية

عند نجاح التشغيل، ستكون لديك:

✅ Backend API يعمل  
✅ Flutter App يعمل  
✅ Integration كامل بينهما  
✅ Text Evaluation يعمل  
✅ UI نظيف وسريع  

**جاهز للاستخدام والتطوير! 🚀**

---

*للمزيد من التفاصيل، راجع: `HOW_TO_RUN.md`*
