# 🚀 كيفية تشغيل BTEC Smart Platform

## دليل التشغيل السريع - خطوة بخطوة

---

## ✅ المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- ✅ Python 3.11+ 
- ✅ UV package manager (`pip install uv`)
- ✅ Flutter 3.0+ ([تثبيت Flutter](https://flutter.dev/docs/get-started/install))
- ✅ Google Chrome (لتشغيل Flutter Web)

---

## 🎯 الطريقة الأولى: التشغيل التلقائي (الأسهل)

### خطوة واحدة فقط:

```bash
./quick-deploy.sh
```

اختر **Option 1: Local Development**

السكريبت سيقوم بـ:
- ✅ فحص المتطلبات
- ✅ إنشاء ملف .env تلقائياً
- ✅ تثبيت dependencies للـ Backend
- ✅ تثبيت dependencies للـ Flutter
- ✅ عرض تعليمات التشغيل

---

## 🔧 الطريقة الثانية: التشغيل اليدوي

### الخطوة 1: تجهيز Backend

```bash
# الانتقال لمجلد Backend
cd backend

# تثبيت Dependencies باستخدام UV
uv sync

# تشغيل Backend
uv run fastapi dev app/main.py
```

**النتيجة المتوقعة:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete
```

✅ Backend يعمل الآن على: http://localhost:8000

### الخطوة 2: اختبار Backend API

افتح نافذة terminal جديدة وجرّب:

```bash
# Test Health Endpoint
curl http://localhost:8000/api/v1/health

# أو افتح في المتصفح:
# http://localhost:8000/docs  (Swagger UI)
```

### الخطوة 3: تجهيز Flutter

افتح نافذة terminal جديدة:

```bash
# الانتقال لمجلد Flutter
cd Flutter

# تثبيت Dependencies
flutter pub get

# تشغيل التطبيق على Chrome
flutter run -d chrome
```

**النتيجة المتوقعة:**
```
Launching lib/main.dart on Chrome in debug mode...
Building application for the web...
Waiting for connection from debug service on Chrome...
This app is linked to the debug service: ws://127.0.0.1:xxxxx/
```

✅ Flutter يعمل الآن في المتصفح!

---

## 📱 ماذا سترى عند التشغيل

### 1️⃣ شاشة تسجيل الدخول

عند فتح التطبيق، سترى:
- 📧 حقل Email
- 🔒 حقل Password
- 🔵 زر "تسجيل الدخول"

**بيانات تجريبية:**
- Email: `test@btec.com`
- Password: `password123`

### 2️⃣ شاشة Dashboard الرئيسية

بعد تسجيل الدخول:
- 📊 إحصائيات سريعة
- 🎯 4 بطاقات رئيسية:
  - تقييم نصي
  - تقييم صوتي
  - النتائج
  - الإعدادات

### 3️⃣ شاشة التقييم

- ✍️ حقل إدخال النص
- 📤 زر "تقييم الإجابة"
- 📊 عرض النتائج بعد التقييم

### 4️⃣ شاشة النتائج

- 📋 قائمة بجميع التقييمات السابقة
- 📅 تاريخ كل تقييم
- ✅ النتيجة المئوية

### 5️⃣ شاشة الإعدادات

- 👤 معلومات الملف الشخصي
- 🔔 إعدادات الإشعارات
- 🌐 تغيير اللغة
- 🚪 تسجيل الخروج

---

## 🧪 اختبار الوظائف

### اختبار Text Evaluation

```bash
# في terminal منفصل
curl -X POST http://localhost:8000/api/v1/btec/evaluate/text \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "student_answer=The quick brown fox&model_answer=The quick brown fox jumps"
```

**النتيجة المتوقعة:**
```json
{
  "status": "ok",
  "data": {
    "similarity": 0.85,
    "levenshtein_ratio": 0.78
  }
}
```

### اختبار عبر Flutter UI

1. افتح شاشة "Assessment"
2. اكتب نص في حقل الإدخال:
   ```
   The quick brown fox jumps over the lazy dog
   ```
3. اضغط زر "تقييم الإجابة"
4. شاهد النتيجة تظهر في بطاقة أسفل الشاشة

---

## 🐳 الطريقة الثالثة: التشغيل بـ Docker

```bash
# بناء وتشغيل الـ containers
docker-compose -f docker-compose.prod.yml up --build

# الانتظار حتى تكتمل العملية (قد تستغرق 5-10 دقائق)
```

**الوصول للتطبيق:**
- Frontend: http://localhost
- Backend: http://localhost:8000
- Swagger UI: http://localhost:8000/docs

---

## 🔍 استكشاف الأخطاء

### Backend لا يعمل

```bash
# تحقق من المنفذ
lsof -i :8000
# أو على Windows:
netstat -ano | findstr :8000

# إذا كان المنفذ مشغول، استخدم منفذ آخر:
uv run fastapi dev app/main.py --port 8001
```

### Flutter لا يعمل

```bash
# نظف وأعد التثبيت
cd Flutter
flutter clean
flutter pub get
flutter run -d chrome
```

### مشاكل Dependencies

```bash
# Backend
cd backend
uv sync --reinstall

# Flutter
cd Flutter
flutter pub get --offline
flutter pub get
```

---

## 📊 المنافذ المستخدمة

| الخدمة | المنفذ | الرابط |
|--------|--------|--------|
| Backend API | 8000 | http://localhost:8000 |
| Swagger UI | 8000 | http://localhost:8000/docs |
| Flutter Web | تلقائي | يفتح في المتصفح |
| PostgreSQL | 5432 | localhost:5432 |

---

## 🎥 فيديو توضيحي

### الخطوات بالترتيب:

1. **تشغيل Backend** (Terminal 1)
   ```bash
   cd backend
   uv run fastapi dev app/main.py
   ```

2. **انتظر حتى ترى:**
   ```
   INFO:     Application startup complete
   ```

3. **تشغيل Flutter** (Terminal 2)
   ```bash
   cd Flutter
   flutter run -d chrome
   ```

4. **انتظر حتى يفتح المتصفح تلقائياً**

5. **استخدم التطبيق:**
   - سجل الدخول
   - اذهب لشاشة Assessment
   - اكتب نص واضغط "تقييم"
   - شاهد النتيجة!

---

## ✅ التحقق من نجاح التشغيل

### Backend
```bash
curl http://localhost:8000/api/v1/health
# يجب أن يعيد: {"status":"ok"} أو مشابه
```

### Flutter
- افتح http://localhost:xxxxx (الرابط الذي يظهر في terminal)
- يجب أن ترى شاشة تسجيل الدخول
- لا أخطاء في Console المتصفح (F12)

---

## 🎯 الخطوات التالية

بعد التشغيل بنجاح:

1. ✅ اختبر جميع الشاشات
2. ✅ جرب Text Evaluation
3. ✅ اختبر Audio (إذا كان متاح)
4. ✅ تحقق من النتائج
5. ✅ راجع الإعدادات

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. راجع `END_TO_END_TEST.md` للاختبارات التفصيلية
2. راجع `SETUP_VERIFICATION.md` للتحقق من الإعداد
3. شغّل `./run-tests.sh` للاختبار التلقائي
4. تحقق من logs في terminal

---

## 🎊 مبروك!

إذا رأيت التطبيق يعمل، فقد نجحت في:
- ✅ تشغيل Backend بنجاح
- ✅ تشغيل Flutter بنجاح
- ✅ ربط Frontend مع Backend
- ✅ اختبار الوظائف الأساسية

**المشروع جاهز للاستخدام! 🚀**

---

*آخر تحديث: 2026-01-02*
